 Project 18: CSS Selectors Quiz

A styled quiz page demonstrating every core CSS selector.

## Topics Covered
- Element selector (`p`, `h1`, `li`)
- Class selector (`.highlight`)
- ID selector (`#header`)
- Descendant selector (`nav a`)
- Grouping selector (`h1, h2`)
- Universal selector (`*`)

git add .
git commit -m "Add Project 18: CSS Selectors Quiz"
git push origin main
